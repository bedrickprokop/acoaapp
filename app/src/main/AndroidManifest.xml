<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.acoaapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".main.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.NavDrawerActivity" />

        <!-- examples -->
        <activity android:name=".main.collaborator.collaborators.CollaboratorsActivity" />
        <activity android:name=".main.collaborator.collaboratordetail.CollaboratorDetailActivity" />
        <activity
            android:name=".main.collaborator.maintaincollaborator.MaintainCollaboratorActivity"
            android:windowSoftInputMode="stateAlwaysVisible" />

    </application>

</manifest>